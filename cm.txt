fix(scripts): actually extract the credential helper, instead of adding a third copy

Correction to the previous commit. It said git_auth.py was "extracted from
git_push_tool.py rather than copied into a second file". It was not — I wrote the
shared module, pointed prune_merged_branches at it, and left git_push_tool's own
inline implementation exactly where it was.

So the state I shipped was worse than the one I described: two live
implementations of the credential dance plus a shared module used by only one
caller. The commit message asserted the opposite, which is the part that matters
— the next person reads the message, believes there is one implementation, fixes
the one they find, and the other keeps its bug.

git_push_tool now uses git_credential_env. Its local _askpass_script,
ASKPASS_USERNAME and the tempfile lifecycle are gone; the only GIT_ASKPASS
mentions left in that file are prose in the module docstring explaining why the
approach is what it is. Unused tempfile import dropped.

Found by checking the claim rather than re-reading the diff: `grep -c ASKPASS`
still returned 7 in a file I had just described as no longer containing it.

Proof: both scripts parse; --help exits 0; a real push through the refactored
path follows this commit.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
