docs: CAPABILITIES' app table still said Supabase for four apps APP_REGISTRY calls migrated

Not drift I had to judge — a contradiction between two boot files stating the
same fact. The handoff updated brain/APP_REGISTRY.md:

    | OASIS AI Platform | ... | Turso `oasis` (migrated from Supabase ...) |
    | PropFlow          | ... | Turso (migrated from Supabase)             |
    | Nostalgic Requests| ... | Turso (migrated from Supabase ...)         |
    | TIKTIK            | ... | Turso (migrated from Supabase ...)         |

and left the PARALLEL table in brain/CAPABILITIES.md reading "React 18, Vite,
Supabase", "Next.js 14, Supabase, Stripe", and so on for the same four apps. Both
files boot every session, so an agent's answer to "what is PropFlow on" depended
on which one it happened to read.

Synced the four rows APP_REGISTRY had already confirmed. Deliberately left every
other row alone: CC Funnel is retired, Lafreniere has no registry row saying
otherwise, and guessing at the rest is precisely what produced this drift.

Tier-1 un-annotated: 53 -> 51.

WHAT I DID NOT DO, and why it is not laziness. 51 Tier-1 hits remain and they are
NOT all drift. A crude pass splits them roughly 26 already-framed-as-legacy to 29
reading-as-current, but the split does not survive contact:

  - "sibling agents subscribe via the shared Supabase agent_events table" is TRUE
    — the event bus stays on LISTEN/NOTIFY by design.
  - "Enforce Supabase RLS" is not simply wrong either; breeze-portal and
    oasis-command-center are still Postgres, where RLS is exactly right. It is
    INCOMPLETE — Turso has no RLS and uses the lib/db_turso.py guard instead.
  - PropFlow genuinely has its own Supabase project per CONTEXT.md, so a row
    naming Supabase there may be correct rather than stale.

Deciding which sentences are still true is a claim about the architecture, not a
find-and-replace, and mass-editing boot files another chassis just authored is
how one agent breaks every other agent's context. The gate now makes that triage
possible; someone with the authority to say what is true should do it.

Proof: doc_sweep re-run shows 51; self_audit clean on all five checks
(graph_matches_disk, docs drift, orphans, broken links, mcp in sync).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
