---
tags: [docs]
last_updated: 2026-07-01
---

# Handover — 2026-07-01  (Mac → Windows)

Everything below is pushed to GitHub and pullable on Windows. Three workstreams:
**SunBiz** (funding CRM + automations), **Live Subs / Command Centre**
(oasis-command-center), and **CredPort** (status only — untouched this session).

> **On Windows, first thing:** `git pull` in each repo. Mac paths are `~/<Repo>`;
> Windows paths are `C:\Users\User\<Repo>`.

---

## ✅ Repo push status (verified 2026-07-01, all on `main`, clean)

| Repo | Path (Mac) | Head commit | State |
|---|---|---|---|
| **oasis-command-center** | `~/oasis-command-center` | `22ea9f8` | pushed, clean — deploys to Vercel prod |
| **SunBiz-Agent** | `~/SunBiz-Agent` | `c04cbde` | pushed, clean — backend on the VPS |
| **CEO-Agent** | `~/CEO-Agent` | this doc | pushed, clean |
| **breeze-portal** (= CredPort) | `~/breeze-portal` | — | clean, synced — **not touched this session** |

---

## ⚡ DO THIS FIRST — turn the SunBiz automations LIVE (one VPS command)

**The finding:** the stage-triggered drips (viewed-application / sent-application follow-ups,
etc.) were **configured + enabled but never firing** — the engine that runs them
(`sunbiz-sequence-runner`) only ever ran on your Windows PC, which was off, so it ran *nowhere*.
Confirmed: **0 autonomous sequence sends ever** in the DB.

**The fix (already committed, `SunBiz-Agent c04cbde`):** the 3 SunBiz daemons are now
`IS_LINUX`-gated so the **always-on VPS owns them** (single instance, no double-send). Backlog
verified safe — **0 overdue rows**, so starting the runner will NOT cold-blast anyone.

**To go live, run on the VPS** (SSH from Windows / the VPS terminal — this couldn't run from the Mac; SSH host is in the guarded ssh config):
```bash
cd ~/SunBiz-Agent && git pull && \
pm2 start ecosystem.config.js --only sunbiz-sequence-runner,sunbiz-lender-response-classifier,sunbiz-cold-outreach-runner && \
pm2 save && pm2 logs sunbiz-sequence-runner --lines 40 --nostream
```
From that moment every lifecycle stage-change fires its sequence **24/7**. Sends go out for real
through `send_gateway` (DNC/CASL/unsubscribe/caps/kill-switch all still apply).

**Verify:** drop a test lead with your own email → move it to `viewed_application` → the 30-min
SMS + 24-hr email should land. Or check the Command Centre **Automations** tab (reads live VPS
heartbeats) — the workers flip to healthy.

**Note — the "sitting too long" gap:** `viewed_application` currently nudges at 30 min + 24 hr on
entry, then stops. For "in the stage too long → keep chasing them to sign," the sequences need
**extending** (48h/72h/5-day steps) — flagged, not yet done (see Pending).

---

## 1. SunBiz — Breeze UW Entry Sheet automation  (`SunBiz-Agent`)

**What it does:** watches Breeze's Google Drive for per-deal UW Sheets → scores against CC's MCA
rules → sends qualifiers to **Ezra's Telegram** with Approve/Deny → on Approve, injects a lead at
the **Live Subs** stage + fires the lifecycle event.

**Status: LIVE on the VPS.** PM2 workers `ezra-telegram-bridge` + `mca-lead-scrubber` up; chain
proven end-to-end (Ezra has made live decisions). **Open gap:** UW sheets carry no email/phone, so
the nurture drip has no channel → a **contact-enrichment** step is the next VPS task. Resume
message: `SunBiz-Agent/docs/breeze-uw-enrichment-resume-message.md` (paste into `claude` on the VPS).

## 2. Live Subs stage + cross-tenant auth  (`oasis-command-center`) — DEPLOYED

- **"Live Subs" pipeline stage** (key `uw_sheet`, above Hot Lead) where approved Breeze deals land.
  Hub: `lib/sunbiz-stage-meta.ts`. Live.
- **Cross-tenant auth fix** — Matt was landing on the OASIS portal after mobile login; root cause
  was `/welcome` hardcoding `?next=/agents`. Fixed + regression-tested. Deployed.
- Open follow-ups (your call): (1) confirm `submissions@sunbizfunding.com` is NOT in Vercel
  `ADMIN_EMAILS`; (2) the "Agents" tab points at the shared `/agent` shell; (3) gate-coverage pass
  on 5 ungated `/t/[slug]` sub-pages. Details in memory `project_oasis_cross_tenant_login_routing`.

## 3. SunBiz email + ad templates + gallery + mobile  (this session) — DEPLOYED

**a) Terms + copy overhaul (`oasis-command-center` + `SunBiz-Agent`).** Every funding tier is now
a locked **1.25 factor**, showing **weekly + total payback** together (e.g. $150K → $2,404/wk /
$187,500 / 18mo; up to $500K), replacing the old off-table figures ($2,083/wk, 36-month, etc.).
Stripped the unconfirmed "no credit pull" claim everywhere; lowered the eligibility gate to
$15K/mo + 6 months. Applied across ~45 email templates + the published Pages site.

**b) 5 SunBiz-original ad creatives** (Tier-Stack story, Hero-Number, Borrow/Pay Rows, Framed Card,
Spotlight) — navy/gold, honest 1.25 numbers. Source: `SunBiz-Agent/templates/ads/` + rendered.

**c) Templates gallery — `oasisai.work/templates`** (nav tab now live in the `/t/sun` portal):
- **Subcategorized by Niche × Campaign-type** — click a niche (restaurant, construction, retail,
  auto, medical, salon, general — **never** the restricted verticals) then a type.
- **Personalize & Download** — fill a merchant's merge fields → live preview → download the
  finished HTML (no code edits).
- **Send to a merchant** — owner/admin only, confirm dialog, **DRY-RUN BY DEFAULT** (no-op until
  you set `LIVE_SEND_EMAIL=1` in Vercel). Routes through `send_gateway` (DNC/CASL/unsubscribe);
  server-renders from the canonical template (no sample-value leaks). **Codex-audited clean** (2 P1s
  found + fixed).

**d) Mobile / half-screen pass** — the lead/application **drawer tab scroll** now works (was a tiny
strip), **7 centered modals portaled** to escape a sidebar-clip at half-screen, and **13
flex-scroll containers** got `min-h-0`. Root-cause pattern logged in memory
`project_oasis_mobile_flex_scroll_bug`.

## 4. CredPort  (`breeze-portal`)  — not touched

Clean + synced on `main`. CredPort = the `breeze-portal` repo (product brand CredPort; 60% OASIS /
40% David; own Supabase). If there's in-flight CredPort work, its handover lives in that chat.

---

## Pending — your decisions (nothing risky ships without your go)

1. **Run the VPS command above** — turns the automations live (the headline item).
2. **`LIVE_SEND_EMAIL=1` in Vercel** — enables the gallery "Send" feature to actually send
   (it's a safe dry-run preview until then).
3. **Extend `viewed_application` / `sent_application` sequences** with 48h/72h/5-day lingering
   follow-ups (the "too long → keep chasing to sign" ask) — quick DB update, not yet done.
4. **Lead-drawer `intent: "transactional"` → `"commercial"`** — same opt-out-suppression gap Codex
   flagged; the lead-drawer sends funding templates but currently skips suppression. Flagged, not
   changed (shared production).
5. **The 3 command-centre auth follow-ups** in §2 (ADMIN_EMAILS check is the quickest).

## Resume checklist (Windows)
1. `git pull` in `SunBiz-Agent`, `oasis-command-center`, `breeze-portal`, `CEO-Agent`.
2. **Automations:** SSH the VPS → run the "DO THIS FIRST" command → verify with a test lead.
3. **Gallery Send:** set `LIVE_SEND_EMAIL=1` in Vercel when ready; send yourself one test first.
4. **SunBiz enricher:** VPS → paste `SunBiz-Agent/docs/breeze-uw-enrichment-resume-message.md`.
5. **Decide** the Pending items (sequence extension, lead-drawer intent, auth follow-ups).
6. **CredPort:** resume in its own chat if needed.

## Obsidian Links
- [[docs/INDEX]]
- [[brain/STATE]]
