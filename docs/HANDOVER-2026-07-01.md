# Handover — 2026-07-01  (Mac → Windows)

Everything below is pushed to GitHub and pullable on Windows. Three workstreams:
**SunBiz** (Breeze UW automation), **Live Subs** (the Command Centre dashboard), and
**CredPort** (untouched this session — status only).

---

## ✅ Repo push status (verified 2026-07-01, all on `main`)

| Repo | Path (Mac flat layout) | State |
|---|---|---|
| **SunBiz-Agent** | `~/SunBiz-Agent` | pushed, clean — backend LIVE on VPS |
| **oasis-command-center** | `~/oasis-command-center` | pushed, clean — deployed to Vercel prod |
| **breeze-portal** (= CredPort) | `~/breeze-portal` | clean, synced — **not touched this session** |
| **CEO-Agent** | `~/CEO-Agent` | synced (this handover + STATE heartbeat) |

**On Windows, first thing:** `git pull` in each repo (Windows paths are `C:\Users\User\...`).

---

## 1. SunBiz — "Breeze UW Entry Sheet" automation  (repo: `SunBiz-Agent`)

**What it does:** watches Breeze's Google Drive for per-deal UW Sheets → scores each against
CC's MCA rules → sends qualifiers to **Ezra's Telegram** (@Dolphin2005_bot) with Approve/Deny →
on Approve, injects a lead into the Command Centre at the **Live Subs** stage and fires the
lifecycle event.

**Status: LIVE and running on the VPS.** Verified 2026-06-30/07-01:
- Both PM2 workers up: `ezra-telegram-bridge` + `mca-lead-scrubber` (config v0.3, $70k true-rev
  floor, full-PII extraction).
- Autonomous chain proven end-to-end: Drive → score → Telegram → Ezra Approve → Live Subs lead →
  `BRAVO_RECORD_STATUS_CHANGED` (8/8 confirmed). Ezra has made 15 live decisions (8 approved).
- Telegram packet now shows the **full funder stack** (all lenders, marked active/paid-off/
  monthly), not just the one active position.

**The one open gap → next task:** UW sheets carry **no email/phone** (blank at underwriting), so
the nurture drip has no channel. The fix is a **contact-enrichment** step. A complete, code-
grounded resume system message is written and committed:
- 📄 `SunBiz-Agent/docs/breeze-uw-enrichment-resume-message.md`

**To resume the VPS work** (from the VPS terminal, not your laptop):
```bash
cd /srv/sunbiz/sunbiz-agent && git pull && claude
```
…then paste that resume message. It tells the agent to build `uw-lead-enricher` (source
email/phone via TruePeopleSearch/web, **never fabricate** — reuses `enrich_leads.py`'s
HIGH/MEDIUM/LOW/CALL_ONLY/NONE confidence model), write it back to the Live Subs lead, and gate
the drip on a real channel; plus a small Drive skip-fix ("Contracts Sent for ME…" was timing out
every tick).

**Original deploy message** (for reference / re-deploy): `SunBiz-Agent/docs/breeze-uw-vps-deploy-message.md`
**Go-live gate:** `SIFT_PARSER_READY=1` in `/srv/sunbiz/ceo-agent/.env.agents` (already set).
**Scoring config (tune here, no code change):** `scripts/scrubber/scoring_config.yaml` `[uw]`.

---

## 2. Live Subs + Command Centre  (repo: `oasis-command-center`)  ⭐ the important one

**Two things shipped and DEPLOYED to production (Vercel):**

**a) "Live Subs" lead pipeline stage** — a new stage directly **above Hot Lead** where the
Ezra-approved Breeze deals land. Internal key `uw_sheet`, label "Live Subs", deep purple.
- Hub edit: `lib/sunbiz-stage-meta.ts` (`LEAD_PIPELINE_STAGES`) — every rail/tab/validator
  derives from it. Also `seeds.ts` enum, `ManifestDashboard` Today's-Focus weight, `format.ts` tone.
- Renders in the pipeline rail, dashboard bar, stage picker. Verified `next build` green + live.

**b) Cross-tenant auth fix** — Matt (SunBiz owner) was landing on the **OASIS** portal after
mobile login. Root cause (proven): `/welcome`'s sign-in links hardcoded `?next=/agents`, so every
non-operator login was handed the OASIS operator surface (the "bridge not connected" page).
Fixed: `app/welcome/page.tsx` (→ bare `/login`) + `lib/auth-routing.ts` (defense-in-depth guard)
+ 3 regression tests in `tests/auth-routing.test.ts`. **Operator (you) still lands on the master
dashboard** — verified. Deployed.

**Open follow-ups (flagged, NOT done — your call):**
1. Confirm `submissions@sunbizfunding.com` is **NOT** in the Vercel `ADMIN_EMAILS` env var
   (would grant Matt operator-preview of all tenants — separate latent leak).
2. The "Agents" nav tab points to the shared `/agent` shell (cosmetic "not connected" on a
   client's own chat) — needs a tenant-scoped page or a relabeled empty state.
3. **Gate-coverage pass:** 5 ungated `/t/[slug]` sub-pages (marketplace/editor/agent) let a
   client user load another tenant's surface — a latent cross-tenant *read* vector. Do this as
   one pass + add a destination guard on `/agents`, extracting a shared confinement util.

Details saved in memory: `project_oasis_cross_tenant_login_routing.md`,
`reference_oasis_manifest_resolution.md`.

---

## 3. CredPort  (repo: `breeze-portal`)

**Not touched in this session.** Repo is clean and synced on `main`. CredPort = the
`breeze-portal` repo (product brand CredPort; 60% OASIS / 40% David; own Supabase). If you have a
separate CredPort chat with in-flight work, that handover lives there — nothing from this chat
changes CredPort code.

---

## Resume checklist (Windows)
1. `git pull` in `SunBiz-Agent`, `oasis-command-center`, `breeze-portal`, `CEO-Agent`.
2. **SunBiz next step:** SSH the VPS → `cd /srv/sunbiz/sunbiz-agent && git pull && claude` → paste
   `docs/breeze-uw-enrichment-resume-message.md` → build the enricher (dry-run first, then enable).
3. **Live Subs:** already live — refresh the dashboard, confirm Matt lands on `/t/sun` on mobile.
4. **Command Centre follow-ups:** decide on the 3 items in §2 (ADMIN_EMAILS check is the quickest).
5. **CredPort:** resume in its own chat if needed.
