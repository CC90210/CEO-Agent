---
description: Quick reference for available tools, skills, and workflows
---

# Capabilities Reference

- **8 MCP servers** — Supabase, Stripe, n8n, Late, Playwright, Context7, Memory, Sequential Thinking
- **14 subagents** — See `brain/AGENTS.md` for full registry and delegation matrix
- **50 skills** — In `skills/[name]/SKILL.md`. Key: systematic-debugging, self-healing, TDD, browser-automation, e2e-testing, cli-anything
- **12 workflows** — In `.agents/workflows/`. Key: `/commit`, `/prime`, `/sync`, `/health`, `/post`, `/debug`, `/cli-anything`
- **Video pipeline** — `scripts/edit_content.py` (FFmpeg 8.0.1, Whisper, ElevenLabs, Remotion)
- **SDK tools** — `python scripts/supabase_tool.py`, `python scripts/stripe_tool.py`
- **Content strategy** — `memory/content-strategy.md` (3 daily pillars, NEPQ framework)
- **Plans** — `.agents/plans/` for implementation plans
