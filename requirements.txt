anthropic==0.85.0
beautifulsoup4==4.14.3
click==8.3.1
cryptography==48.0.1
firecrawl-py==4.22.0
mem0ai==2.0.0b2
numpy==2.4.2
pillow==12.2.0
playwright==1.58.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
requests-oauthlib==2.0.0
requests==2.33.0
stripe==14.4.0
supabase-auth==2.28.2
supabase-functions==2.28.2
supabase==2.28.2
truststore==0.10.4

# V6.0 — optional deps (activated only when migrations 014/015 applied + VPS
# deploy is live). Comment out if you want a minimal local dev install.
fastapi==0.138.0
# starlette is fastapi's transitive ASGI core; pinned explicitly so the CI
# pip-audit gate sees a deterministic version. Bumped 0.48.0 -> 1.3.1 to clear
# 6 starlette CVEs (PYSEC-2026-161, CVE-2025-62727, CVE-2026-48817/48818/54282/54283).
# fastapi 0.138 allows starlette>=0.46 (no upper bound); repo uses only the
# stable FastAPI/Request/JSONResponse surface (no TestClient), validated by a
# clean full-requirements resolve + CI pytest before any VPS deploy.
starlette==1.3.1
uvicorn[standard]==0.32.0
python-multipart==0.0.31
# openai: needs >=1.90.0 for mem0ai 2.0.0b2 compatibility. Upper bound on
# 3.x as a safety net — the SDK has never had a major breaking change
# in the 1.x → 2.x line for our usage (text-embedding-3-small, chat.completions),
# so this range is safe.
openai>=1.90.0,<3.0
# pytest only needed for the CI deploy workflow
pytest==9.0.3
dnspython==2.7.0

# V6 BUILD 2 (2026-05-11) — hybrid retrieval (FTS5 + semantic).
# The retriever degrades to FTS5-only when these are absent, so they're
# soft-required: a fresh install without them works, just without the
# semantic leg. fastembed ships ONNX Runtime directly — NO PyTorch dep
# (sentence-transformers was explicitly rejected for that reason).
# Total disk footprint: ~80 MB model + ~50 MB libs.
fastembed==0.8.0
lancedb==0.30.2
pyarrow==24.0.0

# openai-whisper is NOT listed here on purpose. It pulls in triton + torch
# which frequently fail to install on Windows (needs CUDA / build tools).
# If you need scripts/transcribe.py, install manually:
#     pip install openai-whisper
# On Windows with a CPU-only setup you may need:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#     pip install openai-whisper
libsql>=0.1.11
sqlglot>=30.7.0

# ── Free document-text tier (2026-09-03) ───────────────────────────────────
# HARD-REQUIRED on any host running scripts/integrations/extraction_consumer.py.
#
# These turn a PDF into text with no model and no network, which is what lets
# the extraction daemon keep reading merchant applications while the Claude
# subscription is capped. Before this, the daemon's free fallback tier was
# skipped for application/pdf — and every real job in the queue was a PDF, so
# the fallback was dead on 100% of production traffic (SunBiz outage,
# 2026-09-03).
#
# Three engines on purpose: they fail on different malformed PDFs, and
# lib/doc_text.py keeps whichever returns the most text. Without them the free
# tiers cannot run and a capped subscription is once again a hard outage.
pdfplumber==0.11.9
pypdf==6.15.0
PyMuPDF==1.27.2.3
