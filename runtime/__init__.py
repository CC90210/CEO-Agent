"""Bravo runtime — product infrastructure under brain + memory + skills + scripts.

Orchestrates existing tooling; never duplicates business logic.
Preserves V5.6 outbound chokepoint (`scripts/integrations/send_gateway.py`).
"""
